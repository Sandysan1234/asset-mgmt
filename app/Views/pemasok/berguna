<div class="modal-body">
    <?php if (session('validation')) : ?>
        <script>
            const myModal = new bootstrap.Modal(document.getElementById('exampleModalCenter'));
            myModal.show();
        </script>
    <?php endif; ?> <?= form_open('pemasok/save') ?>
    <?= csrf_field(); ?>
    <div class="row mb-3">
        <label for="kode_vendor" class="col-sm-3 col-form-label">Kode Vendor</label>
        <div class="col-sm-8">
            <input type="text" name="kode_vendor" class="form-control <?= session('validation') && session('validation')->hasError('kode_vendor') ? 'is-invalid' : '' ?>" id="kode_vendor" value="<?= old('kode_vendor') ?>" autofocus required>
            <div class="invalid-feedback">
                <?= session('validation') ? session('validation')->getError('kode_vendor') : '' ?>
            </div>
        </div>
    </div>
    <div class="row mb-3">
        <label for="nama_vendor" class="col-sm-3 col-form-label">Nama Vendor</label>
        <div class="col-sm-8">
            <input type="text" class="form-control" id="nama_vendor" name="nama_vendor" required>
        </div>
    </div>
    <div class="row mb-3">
        <label for="alamat" class="col-sm-3 col-form-label">Alamat</label>
        <div class="col-sm-8">
            <textarea class="form-control" id="alamat" name="alamat" aria-label="With textarea" req></textarea>
        </div>
    </div>
    <fieldset class="row mb-3">
        <legend class="col-form-label col-sm-3 pt-0">Status</legend>
        <div class="col-sm-8">
            <div class="form-check">
                <input class="form-check-input <?= session('validation') && session('validation')->hasError('status') ? 'is-invalid' : '' ?>"
                    type="radio" name="status" id="status1" value="1"
                    <?= old('status', '1') == '1' ? 'checked' : '' ?>>
                <label class="form-check-label" for="status1">
                    Aktif
                </label>
            </div>
            <div class="form-check">
                <input class="form-check-input <?= session('validation') && session('validation')->hasError('status') ? 'is-invalid' : '' ?>"
                    type="radio" name="status" id="status0" value="0"
                    <?= old('status') == '0' ? 'checked' : '' ?>>
                <label class="form-check-label" for="status0">
                    Nonaktif
                </label>
            </div>
            <?php if (session('validation') && session('validation')->hasError('status')) : ?>
                <div class="invalid-feedback d-block">
                    <?= session('validation')->getError('status') ?>
                </div>
            <?php endif; ?>
        </div>
    </fieldset>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button type="submit" class="btn btn-primary">Tambah Data</button>
    </div>
    <?= form_close(); ?>
</div>